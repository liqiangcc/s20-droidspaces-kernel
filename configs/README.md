# Kernel Configuration

`droidspaces.fragment` records the currently known namespace/network gaps observed on the stock SM-G9810 kernel.

It is intentionally small. It must not be treated as the complete DroidSpaces non-GKI configuration. Before a modified build, resolve this fragment against the exact Samsung source tree and compare the final `.config` with the current DroidSpaces kernel requirements.
