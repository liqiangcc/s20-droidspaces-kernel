# Stop Rule

If a proposed change requires broad unrelated kernel/ROM modification to make a single DroidSpaces requirement pass, stop and reassess the architecture instead of accumulating workarounds.
