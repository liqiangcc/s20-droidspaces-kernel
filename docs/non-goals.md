# Non-goals

This project is not trying to maximize kernel features. A successful outcome is the smallest safe kernel delta that satisfies DroidSpaces requirements while preserving the current Samsung firmware behavior.

Specifically, success does not require replacing Android with Linux, building a custom ROM, or enabling every optional container feature in the first kernel candidate.
